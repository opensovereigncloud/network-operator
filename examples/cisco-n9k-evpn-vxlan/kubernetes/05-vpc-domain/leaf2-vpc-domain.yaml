apiVersion: nx.cisco.networking.metal.ironcore.dev/v1alpha1
kind: VPCDomain
metadata:
  name: leaf2-vpcdomain
spec:
  deviceRef:
    name: leaf2
  domainId: 1
  adminState: Up
  peer:
    adminState: Up
    interfaceRef:
      name: leaf2-po1
    switch:
      enabled: true
    gateway:
      enabled: true
    keepalive:
      source:  10.1.1.2
      destination: 10.1.1.1
      vrfRef:
        name: leaf2-vpc-keepalive
