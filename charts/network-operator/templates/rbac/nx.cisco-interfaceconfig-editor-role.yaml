{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "network-operator.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "network-operator.resourceName" (dict "suffix" "nx" "context" $) }}.cisco-interfaceconfig-editor-role
rules:
- apiGroups:
  - nx.cisco.networking.metal.ironcore.dev
  resources:
  - interfaceconfigs
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - nx.cisco.networking.metal.ironcore.dev
  resources:
  - interfaceconfigs/status
  verbs:
  - get
{{- end }}
