apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: network-operator
    app.kubernetes.io/managed-by: kustomize
  name: controller-manager-provisioning-service
  namespace: system
spec:
  ports:
  - name: https
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    control-plane: controller-manager
    app.kubernetes.io/name: network-operator
  type: ClusterIP
