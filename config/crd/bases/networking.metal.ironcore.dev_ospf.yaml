---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: ospf.networking.metal.ironcore.dev
spec:
  group: networking.metal.ironcore.dev
  names:
    kind: OSPF
    listKind: OSPFList
    plural: ospf
    singular: ospf
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.deviceRef.name
      name: Device
      type: string
    - jsonPath: .spec.instance
      name: Instance
      type: string
    - jsonPath: .spec.routerId
      name: Router-ID
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.conditions[?(@.type=="Configured")].status
      name: Configured
      priority: 1
      type: string
    - jsonPath: .status.conditions[?(@.type=="Operational")].status
      name: Operational
      priority: 1
      type: string
    - jsonPath: .status.adjacencySummary
      name: Adjacencies
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OSPF is the Schema for the ospf API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              Specification of the desired state of the resource.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
            properties:
              deviceRef:
                description: |-
                  DeviceName is the name of the Device this object belongs to. The Device object must exist in the same namespace.
                  Immutable.
                properties:
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    maxLength: 63
                    minLength: 1
                    type: string
                required:
                - name
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: DeviceRef is immutable
                  rule: self == oldSelf
              instance:
                description: Instance is the process tag of the OSPF instance.
                maxLength: 63
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: Instance is immutable
                  rule: self == oldSelf
              interfaceRefs:
                description: InterfaceRefs is a list of interfaces that are part of
                  the OSPF instance.
                items:
                  description: |-
                    OSPFInterface defines the OSPF-specific configuration for an interface
                    that is participating in an OSPF instance.
                  properties:
                    area:
                      description: |-
                        Area is the OSPF area identifier for this interface.
                        Must be specified in dotted-quad notation (e.g., "0.0.0.0" for the backbone area).
                        This is semantically a 32-bit identifier displayed in IPv4 address format,
                        not an actual IPv4 address. Area 0 (0.0.0.0) is the OSPF backbone area and
                        is required for proper OSPF operation in multi-area configurations.
                      format: ipv4
                      type: string
                    name:
                      description: |-
                        Name of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      maxLength: 63
                      minLength: 1
                      type: string
                    passive:
                      description: |-
                        Passive indicates whether this interface should operate in passive mode.
                        In passive mode, OSPF will advertise the interface's network in LSAs but will not
                        send or receive OSPF protocol packets (Hello, LSU, etc.) on this interface.
                        This is typically used for loopback interfaces where OSPF adjacencies
                        should not be formed but the network should still be advertised.
                        Defaults to false (active mode).
                      type: boolean
                  required:
                  - area
                  - name
                  type: object
                  x-kubernetes-map-type: atomic
                minItems: 1
                type: array
                x-kubernetes-list-type: atomic
              logAdjacencyChanges:
                description: |-
                  LogAdjacencyChanges enables logging when the state of an OSPF neighbor changes.
                  When true, a log message is generated for adjacency state transitions.
                type: boolean
              providerConfigRef:
                description: |-
                  ProviderConfigRef is a reference to a resource holding the provider-specific configuration of this interface.
                  This reference is used to link the Interface to its provider-specific configuration.
                properties:
                  apiVersion:
                    description: APIVersion is the api group version of the resource
                      being referenced.
                    maxLength: 253
                    minLength: 1
                    pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*\/)?([a-z0-9]([-a-z0-9]*[a-z0-9])?)$
                    type: string
                  kind:
                    description: |-
                      Kind of the resource being referenced.
                      Kind must consist of alphanumeric characters or '-', start with an alphabetic character, and end with an alphanumeric character.
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                    type: string
                  name:
                    description: |-
                      Name of the resource being referenced.
                      Name must consist of lower case alphanumeric characters, '-' or '.', and must start and end with an alphanumeric character.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
                x-kubernetes-map-type: atomic
              routerId:
                description: |-
                  RouterID is the OSPF router identifier, used in OSPF messages to identify the originating router.
                  Follows dotted quad notation (IPv4 format).
                format: ipv4
                type: string
            required:
            - deviceRef
            - instance
            - routerId
            type: object
          status:
            description: |-
              Status of the resource. This is set and updated automatically.
              Read-only.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
            properties:
              adjacencySummary:
                description: |-
                  AdjacencySummary provides a human-readable summary of neighbor adjacencies
                  by state (e.g., "3 Full, 1 ExStart, 1 Down").
                  This field is computed by the controller from the Neighbors field.
                type: string
              conditions:
                description: The conditions are a list of status objects that describe
                  the state of the OSPF.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              neighbors:
                description: Neighbors is a list of OSPF neighbors and their adjacency
                  states.
                items:
                  description: OSPFNeighbor represents an OSPF neighbor with its adjacency
                    information.
                  properties:
                    address:
                      description: Address is the IP address of the remote OSPF neighbor.
                      type: string
                    adjacencyState:
                      description: AdjacencyState is the current state of the adjacency
                        with this neighbor.
                      enum:
                      - Down
                      - Attempt
                      - Init
                      - TwoWay
                      - ExStart
                      - Exchange
                      - Loading
                      - Full
                      type: string
                    interfaceRef:
                      description: InterfaceRef is a reference to the local interface
                        through which this neighbor is connected.
                      properties:
                        name:
                          description: |-
                            Name of the referent.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          maxLength: 63
                          minLength: 1
                          type: string
                      required:
                      - name
                      type: object
                      x-kubernetes-map-type: atomic
                    lastEstablishedTime:
                      description: |-
                        LastEstablishedTime is the timestamp when the adjacency last transitioned to the FULL state.
                        A frequently changing timestamp indicates adjacency instability (flapping).
                      format: date-time
                      type: string
                    priority:
                      description: |-
                        Priority is the remote system's priority to become the designated router.
                        Valid range is 0-255.
                      type: integer
                    routerId:
                      description: RouterID is the router identifier of the remote
                        OSPF neighbor.
                      type: string
                  required:
                  - address
                  - interfaceRef
                  - routerId
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - routerId
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration reflects the .metadata.generation
                  that was last processed by the controller.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
