---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: bordergateways.nx.cisco.networking.metal.ironcore.dev
spec:
  group: nx.cisco.networking.metal.ironcore.dev
  names:
    kind: BorderGateway
    listKind: BorderGatewayList
    plural: bordergateways
    shortNames:
    - bgw
    singular: bordergateway
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.deviceRef.name
      name: Device
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BorderGateway is the Schema for the bordergateways API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              Specification of the desired state of the resource.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
            properties:
              bgpPeerRefs:
                description: |-
                  BGPPeerRefs is a list of BGP peers that are part of the border gateway configuration.
                  Each peer can be configured with a peer type to specify its role in the EVPN multisite topology.
                items:
                  description: BGPPeerReference defines a BGP peer used for border
                    gateway with peer type configuration.
                  properties:
                    name:
                      description: |-
                        Name of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      maxLength: 63
                      minLength: 1
                      type: string
                    peerType:
                      description: |-
                        PeerType specifies the role of this BGP peer in the EVPN multisite topology.
                        FabricExternal is used for peers outside the fabric, while FabricBorderLeaf is used
                        for border leaf peers within the fabric.
                      enum:
                      - FabricExternal
                      - FabricBorderLeaf
                      type: string
                  required:
                  - name
                  - peerType
                  type: object
                  x-kubernetes-map-type: atomic
                minItems: 1
                type: array
                x-kubernetes-list-type: atomic
              delayRestoreTime:
                default: 180s
                description: |-
                  DelayRestoreTime specifies the time to wait before restoring EVPN multisite border gateway
                  functionality after a failure. This allows time for the network to stabilize before resuming
                  traffic forwarding across sites.
                pattern: ^([0-9]+(\.[0-9]+)?(ns|us|µs|ms|s|m|h))+$
                type: string
              deviceRef:
                description: |-
                  DeviceName is the name of the Device this object belongs to. The Device object must exist in the same namespace.
                  Immutable.
                properties:
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    maxLength: 63
                    minLength: 1
                    type: string
                required:
                - name
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: DeviceRef is immutable
                  rule: self == oldSelf
              interconnectInterfaceRefs:
                description: |-
                  InterconnectInterfaceRefs is a list of interfaces that provide connectivity to the border gateway.
                  Each interface can be configured with object tracking to monitor its availability.
                items:
                  description: |-
                    InterconnectInterfaceReference defines an interface used for border gateway interconnectivity
                    with optional object tracking configuration.
                  properties:
                    name:
                      description: |-
                        Name of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      maxLength: 63
                      minLength: 1
                      type: string
                    tracking:
                      description: Tracking specifies the EVPN multisite tracking
                        mode for this interconnect interface.
                      enum:
                      - DataCenterInterconnect
                      - Fabric
                      type: string
                  required:
                  - name
                  - tracking
                  type: object
                  x-kubernetes-map-type: atomic
                minItems: 1
                type: array
                x-kubernetes-list-type: atomic
              multisiteId:
                description: MultisiteID is the identifier for the multisite border
                  gateway.
                format: int64
                maximum: 281474976710655
                minimum: 1
                type: integer
              sourceInterfaceRef:
                description: |-
                  SourceInterfaceRef is a reference to the loopback interface used as the source for the
                  border gateway virtual IP address. A best practice is to use a separate loopback address
                  for the NVE source interface and multi-site source interface. The loopback interface must
                  be configured with a /32 IPv4 address. This /32 IP address needs be known by the transient
                  devices in the transport network and the remote VTEPs.
                properties:
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    maxLength: 63
                    minLength: 1
                    type: string
                required:
                - name
                type: object
                x-kubernetes-map-type: atomic
              stormControl:
                description: |-
                  StormControl is the storm control configuration for the border gateway, allowing to rate-limit
                  BUM (Broadcast, Unknown unicast, Multicast) traffic on the border gateway interface.
                items:
                  properties:
                    level:
                      description: |-
                        Level is the suppression level as a percentage of the interface bandwidth.
                        Must be a floating point number between 1.0 and 100.0.
                      pattern: ^([1-9][0-9]?(\.[0-9]+)?|100(\.0+)?)$
                      type: string
                    traffic:
                      description: Traffic specifies the type of BUM traffic the storm
                        control applies to.
                      enum:
                      - Broadcast
                      - Multicast
                      - Unicast
                      type: string
                  required:
                  - level
                  - traffic
                  type: object
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - traffic
                x-kubernetes-list-type: map
            required:
            - deviceRef
            - multisiteId
            - sourceInterfaceRef
            type: object
          status:
            description: |-
              Status of the resource. This is set and updated automatically.
              Read-only.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
            properties:
              conditions:
                description: The conditions are a list of status objects that describe
                  the state of the Banner.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
