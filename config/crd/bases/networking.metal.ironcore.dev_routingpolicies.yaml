---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: routingpolicies.networking.metal.ironcore.dev
spec:
  group: networking.metal.ironcore.dev
  names:
    kind: RoutingPolicy
    listKind: RoutingPolicyList
    plural: routingpolicies
    shortNames:
    - routemap
    singular: routingpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Routing Policy
      type: string
    - jsonPath: .spec.deviceRef.name
      name: Device
      type: string
    - jsonPath: .status.statementsSummary
      name: Statements
      priority: 1
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RoutingPolicy is the Schema for the routingpolicies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              Specification of the desired state of the resource.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
            properties:
              deviceRef:
                description: |-
                  DeviceName is the name of the Device this object belongs to. The Device object must exist in the same namespace.
                  Immutable.
                properties:
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    maxLength: 63
                    minLength: 1
                    type: string
                required:
                - name
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: DeviceRef is immutable
                  rule: self == oldSelf
              name:
                description: |-
                  Name is the identifier of the RoutingPolicy on the device.
                  Immutable.
                maxLength: 63
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: Name is immutable
                  rule: self == oldSelf
              providerConfigRef:
                description: |-
                  ProviderConfigRef is a reference to a resource holding the provider-specific configuration of this interface.
                  This reference is used to link the Banner to its provider-specific configuration.
                properties:
                  apiVersion:
                    description: APIVersion is the api group version of the resource
                      being referenced.
                    maxLength: 253
                    minLength: 1
                    pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*\/)?([a-z0-9]([-a-z0-9]*[a-z0-9])?)$
                    type: string
                  kind:
                    description: |-
                      Kind of the resource being referenced.
                      Kind must consist of alphanumeric characters or '-', start with an alphabetic character, and end with an alphanumeric character.
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                    type: string
                  name:
                    description: |-
                      Name of the resource being referenced.
                      Name must consist of lower case alphanumeric characters, '-' or '.', and must start and end with an alphanumeric character.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
                x-kubernetes-map-type: atomic
              statements:
                description: A list of policy statements to apply.
                items:
                  properties:
                    actions:
                      description: Actions define what to do when conditions match.
                      properties:
                        bgpActions:
                          description: |-
                            BgpActions specifies BGP-specific actions to apply when the route is accepted.
                            Only applicable when RouteDisposition is AcceptRoute.
                          properties:
                            setCommunity:
                              description: SetCommunity configures BGP standard community
                                attributes.
                              properties:
                                communities:
                                  description: |-
                                    Communities is the list of BGP standard communities to set.
                                    The communities must be in the format defined by [RFC 1997].
                                    [RFC 1997]: https://datatracker.ietf.org/doc/html/rfc1997
                                  items:
                                    type: string
                                  minItems: 1
                                  type: array
                              required:
                              - communities
                              type: object
                            setExtCommunity:
                              description: SetExtCommunity configures BGP extended
                                community attributes.
                              properties:
                                communities:
                                  description: |-
                                    Communities is the list of BGP extended communities to set.
                                    The communities must be in the format defined by [RFC 4360].
                                    [RFC 4360]: https://datatracker.ietf.org/doc/html/rfc4360
                                  items:
                                    type: string
                                  minItems: 1
                                  type: array
                              required:
                              - communities
                              type: object
                          type: object
                          x-kubernetes-validations:
                          - message: at least one BGP action must be specified
                            rule: has(self.setCommunity) || has(self.setExtCommunity)
                        routeDisposition:
                          description: RouteDisposition specifies whether to accept
                            or reject the route.
                          enum:
                          - AcceptRoute
                          - RejectRoute
                          type: string
                      required:
                      - routeDisposition
                      type: object
                      x-kubernetes-validations:
                      - message: bgpActions cannot be specified when routeDisposition
                          is RejectRoute
                        rule: self.routeDisposition == 'AcceptRoute' || !has(self.bgpActions)
                    conditions:
                      description: |-
                        Conditions define the match criteria for this statement.
                        If no conditions are specified, the statement matches all routes.
                      properties:
                        matchPrefixSet:
                          description: MatchPrefixSet matches routes against a PrefixSet
                            resource.
                          properties:
                            prefixSetRef:
                              description: |-
                                PrefixSetRef references a PrefixSet in the same namespace.
                                The PrefixSet must exist and belong to the same device.
                              properties:
                                name:
                                  description: |-
                                    Name of the referent.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  maxLength: 63
                                  minLength: 1
                                  type: string
                              required:
                              - name
                              type: object
                              x-kubernetes-map-type: atomic
                          required:
                          - prefixSetRef
                          type: object
                      type: object
                    sequence:
                      description: The sequence number of the policy statement.
                      format: int32
                      minimum: 1
                      type: integer
                  required:
                  - actions
                  - sequence
                  type: object
                maxItems: 100
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - sequence
                x-kubernetes-list-type: map
            required:
            - deviceRef
            - name
            - statements
            type: object
          status:
            description: |-
              Status of the resource. This is set and updated automatically.
              Read-only.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#spec-and-status
            properties:
              conditions:
                description: The conditions are a list of status objects that describe
                  the state of the RoutingPolicy.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              statementsSummary:
                description: StatementsSummary provides a human-readable summary of
                  the number of policy statements.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
